// adapters/AnalyzeEntryButton.default.jsx
import * as Mod from '../components/AnalyzeEntryButton.jsx';
const C = (Mod && (Mod.default || Object.values(Mod).find(v => typeof v === 'function'))) || (() => null);
export default C;
